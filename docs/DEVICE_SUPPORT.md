# Device Support

BloomOS targets every original Miyoo Mini hardware revision (V1, V2, V3, and V4), the Miyoo Mini Plus, and the Miyoo Mini Flip. These are peers at the product level; revisions may have different capabilities and validation status.

No device is currently certified because BloomOS has not produced its first build. Stable support requires the physical release matrix, including installation, boot, representative game launch, saves and states, shutdown and recovery, native display behavior, and device-specific capabilities.

The maintainer-owned reference hardware currently includes a Mini V2, Plus, and Flip. V1, V3, and V4 support must still be implemented and tested; until equivalent physical evidence is contributed, releases must distinguish "supported" from "maintainer-validated" rather than implying that V2 testing covers all original Mini revisions.

The Miyoo Mini Plus completed a clean install and boot of `v0.1.0-dev-a93f9426` on 2026-08-16. Display, input, Wi-Fi association, and normal shutdown were confirmed on-device. Automated inventory identified model ID 354; it also exposed triple-buffered framebuffer, early-boot Wi-Fi, battery-probe, and FAT dirty-bit reporting/shutdown gaps that are covered by the next hardware-validation build.

Exact device revisions, firmware versions, build commits, test dates, contributor/tester identity or evidence link, and results will be recorded here. A successful boot alone does not qualify as support.

## Physical test evidence

| Date | Hardware | Build | Scope | Result | Follow-up |
| --- | --- | --- | --- | --- | --- |
| 2026-08-16 | Original Mini V2 (model ID 283) | `v0.1.0-dev-14f93ccc` | Fresh FAT32 install, first boot, identity, framebuffer inventory, input inventory, boot log, shutdown | Visually clean install and boot; safe-only request completed; `/dev/input/event0`; virtual framebuffer `640x1440`; no SD I/O failures | Battery capacity was unavailable through power-supply sysfs, so the harness now also captures the existing batmon output. Game launch, saves/states, recovery, and the remaining V1/V3/V4 matrix are still required. |
| 2026-08-16/18 | Miyoo Mini Plus (model ID 354) | `v0.1.0-dev-e61e3fa7`; `v0.1.0-dev-3ed5ba7d` with `dec77ee2` SSH artifacts, staged game-smoke utility, and merged shutdown helper `ef401c19` | Upgrade install, clean-entry boot, identity, display/input/battery inventory, Wi-Fi association, shutdown/reboot, Windows FAT check, key-only SSH, bounded runtime launch for GB/GBC/GBA/NES/SNES/PSX | Clean install and boot; 640×480 physical panel reported separately from 640×1440 framebuffer; Wi-Fi capability true; live AXP battery 74%. Bloom-built static Dropbear 2025.88 accepted repeated Ed25519 logins with the card-pinned host key, refused password-only authentication, replaced tracked listener PID 1049 with PID 2025 during a verified restart, reported model 354, and retained about 74 MB available memory. Six representative homebrew/runtime samples launched through the normal command handoff for eight seconds each; RetroArch stayed alive, exited on SIGTERM, removed the command, and returned to MainUI every time. Peak observed RSS ranged from 10,392 KB (GB) to 38,216 KB (SNES). The SSH validation shutdown returned to Windows Healthy/OK and NOT Dirty without repair. On battery power, a clean reboot returned automatically with fresh uptime and the same host key; telemetry recorded `shutdown_mode=reboot`, successful read-only remount and recursive unmount, and `reboot_command=init`. | Clean shutdown disables SD-backed swap, syncs, and remounts FAT read-only before vendor power control. When USB-powered, the Plus firmware enters its charging screen and requires a second power press; battery-powered reboot is verified. Visual/audio correctness, controls, SRAM and save states, recovery, and longer soak testing are still required. |
| 2026-08-19 | Miyoo Mini Plus (model ID 354) | candidate `v0.1.0-dev-4210275f`; retained known-good `v0.1.0-dev-65a9384a` | USB-powered marked reboot, bounded unconfirmed-update failure, automatic signed rollback, snapshot and health verification | Two healthy candidate boots remained deliberately unconfirmed. The third attempt automatically rebuilt and published the retained signed release, created a verified pre-rollback snapshot, and completed the marked rollback reboot without a power press. The recovered runtime reported `operation: rollback`, `failed_version: 0.1.0-dev-4210275f`, passed structured health, and promoted `65a9384a` back to `known_good` with `recovered_from: 0.1.0-dev-4210275f`; all six snapshots verified. | Repeat reboot and automatic rollback coverage on Mini V2 and Flip. Continue Plus visual/audio/input/SRAM/save-state and soak validation. |
| 2026-08-19 | Miyoo Mini Plus (model ID 354) | `v0.1.0-dev-6b319e30` | Remote bounded runtime and save-lifecycle probes for GB/GBC/GBA/NES/SNES/PSX; 15-minute PCSX-ReARMed soak | Six 60-second probes passed graceful RetroArch `QUIT`, scoped save flush, MainUI return, completed session state, and command cleanup. PCSX-ReARMed then stayed alive for 900 seconds, grew from 2,888 KiB to a 37,856 KiB peak RSS, exited gracefully, preserved the exact 128 KiB SRAM/memory-card file, atomically refreshed its automatic state, and left Play Activity, update, snapshot, storage, and system health green. | Remote evidence does not establish correct video, audio, controls, in-game save semantics, or successful state restore. Physically validate those behaviors and obtain PICO-8/OpenBOR test content. |
| 2026-08-19 | Miyoo Mini Plus (model ID 354) | `v0.1.0-dev-71080921` | Fake-08 guarded launch and repeated state lifecycle using an original 182-byte device-local PICO-8 cartridge | The initial 60-second run peaked at 11,464 KiB and completed through the bounded SIGTERM fallback. A repeated 300-second run with the prior automatic state present peaked at 13,572 KiB and exited through RetroArch `QUIT`. Both runs flushed saves, returned to MainUI, completed the session, removed launch state, and created or refreshed a valid automatic state and screenshot; structured health remained green. | Remotely measured process and file lifecycle does not prove correct rendering, audio, controls, state restoration, or representative-cartridge compatibility. Physically validate those behaviors. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | `v0.1.0-dev-9b5c3aa8` | Signed settings-authority cutover, typed mutation, closed-MainUI reconciliation, rollback/reactivation, USB-powered reboot persistence, FAT shutdown, and health | The signed candidate promoted to known-good. Bloom authority advanced from generation 4 to 9 across guarded transitions; mutation materialized, the compatibility writer reconciled without losing Bloom authority, direct legacy sync failed closed, rollback and repeated operations were idempotent, and the baseline preference was restored. Automatic reboot returned without a power press, retained generation 9 byte-for-byte, remounted FAT read-only, and passed aggregate health. | Repeat authority persistence and recovery coverage on Mini V2 and Flip; closed MainUI remains a service-owned compatibility writer until replacement. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate library service based on `c65e8cd4`; installed known-good `v0.1.0-dev-9b5c3aa8` | Candidate-only canonical Onion system/application import through an exact-hash ARM binary and signed 53-system package mapping | The service created the durable schema, atomically imported all 53 installed emulator configs and 23 installed application configs as generation 1, returned the same counts through bounded status, and made the repeated import a generation-preserving no-op. A caller with no inherited library path succeeded through the corrected `bloomctl library` wrapper. Pre-test aggregate health was green. | This did not activate an unsigned update or cut over MainUI. Signed update, reboot persistence, malformed on-device input, games/favorites/recents, query performance, and consumer cutover remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate game-index service based on `31e77216`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash ARM game enumeration, repeat invalidation, aggregate status, system-filtered paging, and cross-system cursor paging | The first scan indexed 22 installed games across 53 systems and advanced the durable library from generation 1 to 2 with zero errors. The repeat scan reported unchanged and retained generation 2. Bounded three-item pages returned stable GameID cursors both globally and for GBA; the next global page resumed without duplication. Temporary candidate files were removed and aggregate health remained green. | This candidate-only run did not activate an unsigned update or cut over MainUI. Signed-update/reboot persistence, favorite/recent migration, larger on-device performance measurement, and Bloom UI consumption remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate legacy-state service based on `938d5e3d`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash ARM schema 2-to-3 migration and deterministic read-only favorite/recent import against the 22-game canonical index | A temporary four-entry favorite fixture produced one exact GameID match, one duplicate, one unmatched ROM, and one invalid app-type entry. The live hidden recent list contributed 17 invalid app-only entries and no false game matches. The repeat import returned identical counts. The original schema-2 database, empty favorite list, and hidden recent list were restored byte-for-byte after the test. | This candidate-only run did not activate an unsigned update, change MainUI authority, or validate signed-update/reboot persistence. Bloom-owned favorite/recent consumers and direct resume remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-power` service based on `632fd88f`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash read-only power policy/status probe through the installed `bloom-platform` and low-level clean shutdown dependency | The candidate identified `mini_plus`, reported the firmware poweroff strategy, confirmed reboot support, and was removed after the probe. Upload and device SHA-256 matched. No power mutation was requested. | Existing Plus clean-FAT and automatic reboot evidence applies to the unchanged low-level helper, not yet to an installed signed `bloom-power` caller chain. Signed activation/reboot and Mini V2/Flip adapter validation remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-network` service based on `b91bdd43`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash read-only capability, firmware Wi-Fi preference, and sysfs association probe over the existing connection | The first probe found that the firmware `jsonval` requires the Miyoo runtime library path. The corrected exact-hash candidate reported Wi-Fi capable, enabled, and associated through bounded JSON without SSID/address disclosure, then was removed. No network state was changed. | Install through a signed update, confirm reboot persistence, validate disconnected/disabled states on hardware, and repeat applicable behavior on Flip and original Mini. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-network` reconciliation based on `822f624b`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash fixed reconciliation handoff to the inherited compatibility backend | Reconciliation applied successfully and preserved Wi-Fi preference, `wlan0` up/carrier state, WPA supplicant, DHCP client, and SSH reachability. The candidate was removed and its upload/device hashes matched. | This does not validate user-facing Wi-Fi mutation or replacement of the inherited backend. Signed-update/reboot and disabled/disconnected state coverage remain pending, along with Flip/original Mini validation. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-network` preference control based on `b8568558`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash bounded enable request through active Bloom settings authority and fixed apply backend | The already-enabled request reported saved/applied, retained the canonical settings file byte-for-byte, and remained associated. The candidate was removed and upload/device hashes matched. Disable was intentionally not invoked over the sole SSH transport. | Validate disable/re-enable locally through signed UI, disconnected recovery, reboot persistence, backend replacement, and applicable Flip/original Mini behavior. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-controls` brightness adapter based on `e1ecd1bf`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash bounded brightness status and idempotent current-level apply through active Bloom settings | Status reported raw PWM 35. Requesting the current logical level 7 persisted/applied successfully while retaining both PWM 35 and the canonical settings file byte-for-byte. The candidate was removed and upload/device hashes matched. | Signed boot-path installation, visible range/legibility review, rapid adjustment latency, and Mini V2/Flip behavior remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-time` service based on `720ad052`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash capability-aware clock status and reconciliation | Status reported RTC present and a usable clock. Reconciliation selected `rtc_preserved`, made no wall-clock change during the probe, retained the RTC capability flag, and removed the hash-matched candidate. | Signed boot-path installation and reboot persistence remain pending. Physically validate the saved-time fallback on original Mini and repeat RTC behavior on Flip. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-volume` service based on `9317d349`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash source-built firmware audio-server FIFO status and idempotent absolute-volume request | The candidate identified the live FIFO, applied the already-effective raw -60 level, retained audioserver PID 844, and left canonical settings byte-identical. The candidate was removed after the probe. | Physically validate audible range, mute semantics, rapid buttons, resume restoration, signed installation, and applicable Mini V2/Flip behavior. Relative audio boost is intentionally unavailable through the write-only fallback. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-lid` service based on `cb3c791c`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash read-only lid capability/status probe | The candidate returned the explicit non-lid `unsupported` state and was removed after its upload hash matched. No power, display, input, or settings state changed. | Physically validate both Flip hall-sensor layouts and open/close transitions before integrating suspend/resume policy. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-wifi` SHA-256 `5ec86c0d09d25dd7dcb8196725c9e20935478f1cab582a3bb5952e5a04af4c35`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash Bloom-owned reconciliation of an already-associated Wi-Fi connection | The candidate returned `preserved` with no lifecycle command, retained settings SHA-256 `24c35391101d40789feac06966bb6ce75063cded0d0178b21c75f72744e0dd8d`, retained WPA PID 953 and DHCP PID 1034, kept `wlan0` up with carrier, and preserved the SSH session. The candidate was removed. | Validate disconnected start and local disable/re-enable through a signed build. Implement and physically validate the Flip backend; replace the remaining inherited auxiliary-service compatibility path. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | candidate `bloom-shell` SHA-256 `9f22985b1c951ff281de3c3cfbce89b22daf72d8e88c87d523723e1e89e00c2f`; installed known-good `v0.1.0-dev-9b5c3aa8` | Exact-hash read-only native shell catalog probe | The source-built ARM binary loaded the live Bloom library directly through bounded SQLite paging and reported one installed Game Boy game. It made no display, input, launch, MainUI, settings, ROM, or database mutation and was removed after its upload hash matched. | Install through a signed development build and physically validate Home, GB browsing, launch, direct return, crash fallback, text, and input latency before advancing the shell gate. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed candidate `v0.1.0-dev-5e81cfef`; retained known-good `v0.1.0-dev-ce1d7b23`; exact-hash shells from signed `v0.1.0-dev-5ce47eed`, `v0.1.0-dev-d05689d9`, and `v0.1.0-dev-ee238ae9` | First physical Bloom Shell boot with canonical GB library, recents, and favorites consumers; remote framebuffer correction | The signed candidate installed and reached Bloom Shell with one indexed GB game, but the complete interface rendered 180 degrees from the physical panel. The first corrective shell made the resting page upright, while every button temporarily exposed older upside-down pages. This confirmed that the physical 640x480, virtual 640x1440 framebuffer requires both a 180-degree transform and publication to all three pages. Repeated SDL flips in `d05689d9` cleared stale content but a full 3,686,400-byte SSH capture showed rendered data only in page 0. The exact signed `ee238ae9` shell explicitly published the converted frame; a second full capture produced the same SHA-256 `145fcb86b106bc702e3648a06781acdfa43d72e51fa03e907d20461e67a4d563` for each of the three 1,228,800-byte pages. None of these builds was confirmed, and the verified pre-update snapshot, known-good release, and prior exact-hash shell binaries remain retained. | Remote framebuffer equality proves page contents, not physical panel readability or input-transition appearance. Defer that physical check until the maintainer returns. Browse, launch, return, text, controls, audio, SRAM, saves, and states remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed GameSwitcher candidate from `v0.1.0-dev-880d2492`; SHA-256 `44be8c5f46d4a0accd9c0408fd15c564f8a11e6d22fcc413b3d335330f73baa3` | Privacy-safe canonical-recents startup probe against the live Bloom catalog | The exact 180,488-byte ARM binary loaded the durable catalog and returned schema 1, service `game-switcher`, ready true, and zero canonical recents. That agrees with the earlier deterministic legacy import, which found no valid game recents in the app-only hidden list. The temporary candidate was removed, Bloom Shell remained running, and the unconfirmed update and rollback state remained unchanged. No display, input, game launch, recent mutation, or installed GameSwitcher replacement occurred. | Physically validate the signed GameSwitcher UI, launch/resume, removal and ordering behavior, and direct return once the framebuffer transition check is complete. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed GameSwitcher selection candidate from `v0.1.0-dev-706ebf19`; SHA-256 `a2df9758e15f5d2e79c6d92efd07428f3002cf5be37372fdcfd3c7b4569134a0` | Exact-hash ARM readiness after canonical selection and transactional ordering cutover | The exact 196,996-byte binary loaded the live catalog and returned schema 1, service `game-switcher`, ready true, and zero canonical recents. The candidate therefore could not exercise selection without fabricating user state; it was removed instead. Bloom Shell PID 1184 remained active and the unconfirmed update state stayed byte-for-byte equivalent at the structured boundary. | Host tests prove that a selected canonical GameID publishes its own validated launcher/ROM command, moves to position zero without uniqueness collisions, and removes the command if promotion fails. Physical selection, save/session lifecycle, direct return, and removal remain pending. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed shell candidate `v0.1.0-dev-1349de6f`; shell SHA-256 `83eebd22350d8e16661c6f66c2f196d890ccfc6770d5ab0c016e8084dc56e094`; status adapter SHA-256 `98046f04fea1a112d1ae516f54e9e90f828fc8a0d9dd13b80ae354c359b23515` | Exact signed Settings-health readiness through a dedicated bounded adapter | The Ed25519-signed 203,031,673-byte release archive verified at SHA-256 `27be65d86d9044e6b01c0e1a7257e8d33f041a57492bac8a04e644bf0ee9813c`. Its exact 79,816-byte shell and 1,739-byte adapter were transferred with matching device hashes. The adapter returned healthy system, testing updater, and enabled/ready RetroAchievements state in 0.65 seconds; the shell probe then returned ready, `health_ready: true`, and `healthy: true` with one GB game. Both candidates were removed, installed shell PID 1184 remained active, and the prior update stayed unconfirmed. | This proves signed binary loading and bounded Settings data on Plus, not physical rendering or transitions. Physically recheck the three framebuffer pages, browse Settings, and validate input latency after returning to the device. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed Apps shell candidate `v0.1.0-dev-1fa75806`; shell SHA-256 `76369a9b3cae75c1b5d76a8991ae9e991cefc2c38385e12821f93741a9124584` | Canonical application catalog probe and controlled framebuffer transition capture | The signed shell loaded all 23 present canonical applications and returned ready with healthy bounded status. Separately, one injected R1 navigation event moved the installed shell from Home to Library without launching content. Immediate, one-second, two-second, and restored-Home captures each had three identical 1,228,800-byte visible-page hashes. Rotating the captures 180 degrees for inspection showed coherent upright Home and Library frames; Back restored the exact original 4 MiB framebuffer SHA-256 `542de8214bcc5f3de6dae7ad80db644ad4c304928254ec59c732f1d855e2ebc7`. Temporary files were removed, shell PID 1184 remained active, and the prior update stayed unconfirmed. | Remote captures now cover a controlled navigation transition and all framebuffer pages, but do not prove the physical LCD scanout between captures. Physically confirm the transition later. App launching remains intentionally disabled until a supervised app-return contract exists. |
| 2026-08-21 | Miyoo Mini Plus (model ID 354) | signed library candidate `v0.1.0-dev-9a9611ae`; binary SHA-256 `9095302c90466747c26c234400e6743b71c735b3b4c36daebec3567d1c48c6b8` | Isolated schema-3 to schema-4 application compatibility migration | The exact signed ARM binary ran inside a temporary chroot against a SQLite backup of the live catalog. It retained ready generation 2 with 53 systems, 22 games, and 23 apps, and conservatively classified all 23 existing rows as `mainui-dependent`. The live catalog remained schema 3 and its SHA-256 stayed `9ec33e433a1095d76e912eca8b125e564db65ad819cc478920e4522bb84e741b`; shell PID 1184 and the unconfirmed updater state were unchanged. The temporary device root was removed. | Assign reviewed explicit classifications before enabling any app launch. Validate supervised launch/return only for permitted classes; keep MainUI-dependent apps unavailable in Bloom Shell. |
| 2026-08-22 | Miyoo Mini Plus (model ID 354) | signed and confirmed `v0.1.0-dev-e98840d9`; workflow run 32556360485 | UX-convergence root, Games, flat Settings, Quick Settings, Bloom GameSwitcher, no-shoulder invariant, B/MENU return paths, signed update, aggregate health, 30-minute shell soak, and 500-cycle host regression | The 203,077,320-byte signed archive independently validated at SHA-256 `3fcd4a347564572811900f928d71d74734ad2454e4511375fd61d262fa52fa8c`, installed through the guarded updater, booted once, and promoted to known-good with MainUI absent and Bloom Shell live. Aggregate health is green; the canonical catalog remains ready with 53 systems, 22 games, and 23 apps. Remote framebuffer/input checks reached all five root destinations, changed Games from GB to GBA, opened flat Settings and Quick Settings, and showed L1/R1/L2/R2 as exact no-ops on root, Games, Settings, and GameSwitcher. MENU opened the Bloom-owned GameSwitcher without Onion branding, START toggled its Quick Settings, MENU restored the exact prior root frame, and B returned to that same frame. A preceding 30-minute shell soak held PID, 6,900 KiB RSS, 22,564 KiB VmSize, and nine file descriptors constant across 61 samples. Host regression passed 232 native tests, 392 shell tests, and 500 root/open/action/back cycles. Machine-readable evidence: [`validation/shell/plus-ux-convergence-20260822.json`](validation/shell/plus-ux-convergence-20260822.json). | This is remote framebuffer and injected-input evidence, not a physical UX pass. Physical LCD transition appearance, readability, tactile held-scroll feel, audio, representative game saves/states, GameSwitcher actions with a real recent, Mini V2, and Flip remain pending. |
| 2026-08-22 | Miyoo Mini Plus (model ID 354) | installed known-good `v0.1.0-dev-e98840d9`; boot-reconciliation follow-up | Upgrade app-classification reconciliation and supervised Bloom-native app launch/return | The signed payload had installed reviewed declarations, but the existing schema-4 catalog still classified all 23 apps as MainUI-dependent. After a retained exact-hash database backup, the public transactional import advanced generation 2 to 3 and produced two Bloom-native, one development-only, and 20 hidden MainUI-dependent rows; a repeat was a generation-preserving no-op. After a clean reboot, Quick Guide launched under its own `launch.sh`/`infoPanel` processes, rendered upright BloomOS help on all three framebuffer pages, and B returned directly to the exact prior Bloom root hash without MainUI. Aggregate health remained green. The follow-up makes the bounded import part of boot before Bloom Shell reads the catalog and removes game-only controls from the Apps footer. Machine-readable evidence: [`validation/shell/plus-app-supervision-20260822.json`](validation/shell/plus-app-supervision-20260822.json). | The automatic boot follow-up is host-tested, not installed by this evidence. Activity Tracker and other reviewed apps, malformed-theme fallback, tactile controls, and physical LCD transition behavior remain pending. |
| 2026-08-22 | Miyoo Mini Plus (model ID 354) | installed known-good `v0.1.0-dev-e98840d9`; read-only launcher audit | PICO-8 and ScummVM structured-launch classification | The live 280-byte PICO launcher resolves `fake08_libretro.so` (SHA-256 `1537ce580e2c14e8eafed0317a053d54b8edce9ee403e412d5be596e774d65ce`) and the 581-byte ScummVM launcher resolves `scummvm_libretro.so` (SHA-256 `dee2e1c6b5a76b3702651acbd9d13763c58dc77440d84fc755c003c3fb5d61d1`). Both are therefore eligible for Bloom's existing structured RetroArch session and scoped-save boundary. The installed shell probe reported nine structured systems and 19 games; aggregate health and known-good update state passed before the audit. | This read-only classification did not launch a game or prove physical rendering, controls, save semantics, or restore. Those systems still require signed integrated and physical coverage. Ports remain hidden pending a standalone lifecycle/durability boundary. |
| 2026-08-16 | Miyoo Mini Flip (model ID 285) | `v0.1.0-dev-bc086aa6`, `v0.1.0-dev-95de9362`, `v0.1.0-dev-fbf1a03e`, `v0.1.0-dev-3ed5ba7d` | Upgrade install, identity, display/input/battery/lid inventory, Wi-Fi capability, clean-entry boot, instrumented shutdown, Windows FAT check, Play Activity WAL compatibility, key-only SSH | Clean install and boot; native 752×560 panel; two input devices; hall sensor present with open-lid raw value 1; live AXP battery 64%; Wi-Fi capability true. A controlled shutdown cycle entered and returned clean. Play Activity converted a seeded rollback-journal database to WAL, preserved its rows, passed integrity and quick checks, and left no WAL/SHM sidecars after three UI opens. Bloom-built static Dropbear 2025.88 accepted repeated Ed25519 logins, refused password-only authentication, reported model 285 over the live harness, and retained about 74 MB available memory. | Earlier cycles entered or returned dirty, although CHKDSK found no filesystem damage. The first Activity Tracker open/exit briefly glitched the display, then two repeats were normal with no logged crash. Longer soak testing, reboot-path isolation, reboot-time SSH activation, and lid behavior remain required. |

The V2 result is a maintainer validation baseline, not certification of V2 or evidence for V1, V3, or V4. The first Plus shutdown retest was invalid because the card entered Linux dirty after a failed Windows command-line eject. After repairing and using Windows Safely Remove Hardware, the card entered Linux without the not-properly-unmounted warning and returned to Windows clean after BloomOS shutdown.

On 2026-08-18, an experimental source-built PCSX-ReARMed standalone executable
passed an isolated ARM ABI and dynamic-library smoke test on the Plus. BloomOS
subsequently excluded that redundant package in favor of its existing
PCSX-ReARMed libretro integration, so the result is retained only as historical
engineering evidence and is not a support claim for the shipped core.

On 2026-08-17, a Plus test invoked BloomOS's clean `shutdown -r` path
over SSH. The helper quiesced SD-card consumers and disconnected normally, but
the device did not return to its prior address within five minutes and a subnet
SSH scan found no replacement address. After manual power-on, the device
returned at `192.168.1.180` with the same card-pinned host key and passed the
guarded identity and read-only smoke checks. The persistent internal shutdown
log records `swapoff_complete=1`, successful first-attempt read-only remount,
`remount_ro_final=0`, and `umount_recursive=0`; the subsequent redundant final
unmount returned `1` after the recursive unmount had already succeeded. This
initially appeared to isolate the remaining defect to reboot/power control after
clean storage quiescing. Follow-up testing on 2026-08-18 established that the
device was externally powered: after shutdown, Plus firmware entered its normal
charging screen, where the first power press wakes charging display and a second
press boots Linux. With USB-C disconnected, the merged mode-file helper rebooted
the Plus automatically. It returned to SSH with fresh uptime and the persistent
log recorded `shutdown_mode=reboot`, `remount_ro_final=0`,
`umount_recursive=0`, `reboot_command=init`, and
`reboot_init_returned=0`. The direct kernel fallback was correctly unnecessary.
